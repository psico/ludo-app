import {
    makeStyles
} from '@material-ui/core';

const useStyles = makeStyles(() => ({
    root: {
        display: "flex",
        justifyContent: "center",
        background: "#3f51b5"
    }
}));

export default useStyles;
